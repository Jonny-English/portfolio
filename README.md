# Portfolio - Conversational Personal Website

这是一个基于 **Next.js 15**, **Tailwind CSS v4**, 和 **TypeScript** 构建的极简风格个人网站。它结合了对话式交互（Chat UI）与传统的浏览体验。

## ✨ 特性

- **对话式首页**：仿 AI 聊天界面，支持 `/about`, `/projects` 等指令。
- **极简设计**：深受 Claude 界面启发，强调留白、排版与阅读体验。
- **深色模式**：完美适配 Light/Dark 主题，自动记忆用户偏好。
- **响应式布局**：移动端优先，在桌面端提供优雅的双栏布局。
- **Markdown 渲染**：内置文章渲染支持（基于 `react-markdown`）。
- **Mock 数据驱动**：所有内容通过本地 JSON/TS 文件管理，易于替换。

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看结果。

## 📂 目录结构

```
src/
├── app/                  # Next.js App Router 页面
│   ├── globals.css       # 全局样式与 Tailwind 变量
│   ├── layout.tsx        # 根布局与 ThemeProvider
│   ├── page.tsx          # 首页 (Chat Interface)
│   ├── projects/         # 项目列表页
│   └── writing/          # 文章列表与详情页
├── components/
│   ├── content/          # 内容卡片 (AboutCard, ProjectList...)
│   ├── features/         # 核心功能组件 (ChatFeed, Sidebar...)
│   ├── providers/        # Context Providers
│   └── ui/               # 基础 UI 组件 (Card, Button...)
├── data/                 # 网站内容配置 (Profile, Projects, Posts)
├── hooks/                # 自定义 Hooks (useCommander)
└── lib/                  # 工具函数
```

## 🛠️ 如何自定义

### 修改个人信息
编辑 `src/data/profile.ts` 修改你的名字、简介和社交链接。

### 添加项目
编辑 `src/data/projects.ts` 添加你的作品集。

### 写文章
编辑 `src/data/writing.ts` 添加文章元数据和内容。
*(进阶：你可以将其改造为读取本地 `.md` 文件或 CMS API)*

### 调整设计与字体
查看 `src/app/globals.css` 中的CSS变量。默认使用 `Inter` (无衬线) 和 `Newsreader` (衬线)。

## 📦 部署

本项目专为 **Vercel** 优化，零配置即可部署。

1. 将代码推送到 GitHub。
2. 在 Vercel 导入项目。
3. 点击 **Deploy**。

## 🤖 扩展建议

- **接入真实 AI**：利用 Vercel AI SDK 将 `useCommander` 替换为真实的 LLM 调用。
- **CMS 集成**：将 `data` 目录替换为 Contentful 或 Sanity 接口。
- **Analytics**：添加 Vercel Analytics 以追踪访问数据。

---

© 2026 Your Name. MIT License.
